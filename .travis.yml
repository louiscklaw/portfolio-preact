# https://docs.travis-ci.com/user/docker/

#.travis.yml
dist: bionic

language: node_js
node_js:
  - 10
cache: yarn

# install:
#   - yarn build

stages:
  - build
  - deploy
  - merge

import:
  - .travis/env.yml
# TODO: resume me before pushing to test branch
#   - .travis/build.yml
#   - .travis/deploy.yml
#   - .travis/merge.yml

script:
  - exit 1

after_failure:
  - mkdir -p .travis
  - cd .travis
  - wget https://raw.githubusercontent.com/louiscklaw/travis-playlist/master/travis-broadcast/broadcast.sh
  - bash broadcast.sh need_help "build failed when building portfolio-preact on master branch"
# after_success:
#   - cd _util
#   - id
#   - sudo apt update
#   - sudo apt install -y python3 python3-pip python3-dev python3-wheel python3-setuptools
#   - pip3 install --user pipenv
#   - pipenv sync
#   - pipenv run python3 ./merge.py
#   - cd ..
